# GyenBox Desktop Client

Electron desktop sync is intentionally scoped as a later phase. The web API contracts in `packages/api-client` reserve endpoints and event types for selective sync, pause/resume, bandwidth throttling, and LAN sync.
